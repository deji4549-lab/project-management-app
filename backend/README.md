# Backend — project-management-app

This minimal Express backend serves the frontend and provides a sample API endpoint.

Requirements
- Node.js (14+)

Install and run

```powershell
cd backend
npm install
npm run dev   # development with nodemon
npm start     # production
```

Health check: `GET /health`
Sample API: `GET /api/projects` returns mock project data
